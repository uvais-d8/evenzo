export enum VendorStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected'
}

export enum UserStatus {
    ACTIVE = 'active',
    BLOCKED = 'blocked'
}

export enum Role {
    ADMIN = 'admin',
    VENDOR = 'vendor',
    USER = 'user'
}
